# Example environment variables for Node.js + MySQL application
# Copy this file to `.env` and fill secrets appropriately before running in production.

# Server
NODE_ENV=development
PORT=3000

# Database (MySQL)
# For local/docker development you can use the service name defined in docker-compose
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=devops123
DB_NAME=devops_db

# (Optional) MySQL root password used by test/dev containers only
MYSQL_ROOT_PASSWORD=devops123

# App settings
LOG_LEVEL=debug
API_TIMEOUT=30000
MAX_CONNECTIONS=10

# Security / Auth
# Do NOT commit real secrets to the repository. Provide these via CI secrets or runtime env.
JWT_SECRET=change_me

# Example external services
REDIS_URL=redis://localhost:6379

# Docker: Uncomment to override build-time behavior (not recommended for production)
# SKIP_ENV_COPY=true
